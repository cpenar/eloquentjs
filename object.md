```coffee
fun.apply(thisArg, [argsArray]) # pure ; applique this value and arguments provided as an array
fun.call(thisArg[, arg1[, arg2[, ...]]]) # pure ; applique this value and arguments provided individually
```
