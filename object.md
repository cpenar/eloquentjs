```coffee
fun.apply(thisArg, [argsArray]) # pure ; calls a function with a given this value and arguments provided as an array
fun.call(thisArg[, arg1[, arg2[, ...]]]) # pure ; calls a function with a given this value and arguments provided individually
```
