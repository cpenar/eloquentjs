```coffee
fun.apply(thisArg, [argsArray]) # applique this value and arguments provided as an array
fun.call(thisArg[, arg1[, arg2[, ...]]]) # applique this value and arguments provided individually
Object.getPrototypeOf(obj) # 
aObj.prototype # 
```
